import React from 'react';

export const Caracteristicas = ({opciones, tipoComercio}) => {

    //const { cafeterias, restaurantes, heladeria, muebles, electrodomesticos, ferreterias, ventasConstruccion, materialesElectrico, bombas } = JSON.parse(opciones);

    //console.log(JSON.parse(opciones))

    //const { cafeterias, restaurantes, heladeria } = JSON.parse(opciones);

    return (
        /*
        <div>
            { (cafeterias && tipoComercio == 0) && <p className="card-text">Cafeterias</p>}
            { (restaurantes && tipoComercio == 0) && <p className="card-text">Restaurante</p>}
            { (heladeria && tipoComercio == 0) && <p className="card-text">Heladeria</p>}
            { (muebles && tipoComercio == 1) && <p className="card-text">Muebles</p>}
            { (electrodomesticos && tipoComercio == 1) && <p className="card-text">Electrodomesticos</p>}
            { (ventasConstruccion && tipoComercio == 2) && <p className="card-text">Ventas de materiales de construccion</p>}
            { (materialesElectrico && tipoComercio == 2) && <p className="card-text">Meteriales Electrico</p>}
            { (bombas && tipoComercio == 2) && <p className="card-text">Bombas</p>}
        </div>
        */
       <div>
           hola
       </div>
    )
}
